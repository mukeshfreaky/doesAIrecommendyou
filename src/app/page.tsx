import { ScanFlow } from "@/components/ScanFlow";

export default function HomePage() {
  return (
    <main className="min-h-screen py-10 sm:py-16 px-4 sm:px-8 flex flex-col items-center justify-start">
      <ScanFlow />
    </main>
  );
}

